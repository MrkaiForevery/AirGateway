# AirGateway 架构设计说明书

| 项目属性 | 内容 |
|---------|------|
| 项目名称 | AirGateway 响应式多协议 API 网关 |
| 版本 | v1.0-SNAPSHOT |
| 技术栈 | Java 17 / Spring Boot 3.2.12 / Spring Cloud 2023.0.3 / Spring Cloud Alibaba 2023.0.3.2 / Spring WebFlux / Project Reactor |
| 源代码规模 | 134 个 Java 文件，100 个子包 |
| 构建工具 | Maven |

---

## 1. 项目概述

### 1.1 项目背景

AirGateway 是一个面向微服务架构的响应式 API 网关。项目旨在替代标准 Spring Cloud Gateway 默认的过滤器链模型，构建一套**策略驱动、多协议支持、多注册中心并发、可热刷新配置**的企业级网关基础设施。

### 1.2 设计目标

| 目标 | 说明 |
|------|------|
| 响应式架构 | 基于 Spring WebFlux + Project Reactor，全链路非阻塞，适配高并发低延迟场景 |
| 多协议接入 | 统一接入 REST / SOAP / WebSocket / SMPP / AI / 管理后台等多类 API |
| 多注册中心并发 | 同时对接 Nacos / ZooKeeper / Consul / Eureka 等多个注册中心，支持异构注册中心混用 |
| 策略驱动过滤器 | 过滤器编排采用策略模式，运行时可通过配置热刷新过滤器执行顺序 |
| 热刷新流量控制 | 限流 / 熔断 / 降级规则通过 Nacos 动态下发，管道式组合多种算法 |
| 可观测性 | 集成 Micrometer + Prometheus，提供访问日志、业务日志、健康检查等多维度监控 |

### 1.3 当前项目状态

> **注意**：项目处于活跃开发阶段。`AirGatewayEngine` 中各路由方法目前返回硬编码 mock 响应，请求转发至后端服务的核心链路尚未完全接通。多注册中心、服务自注册、流量控制框架、消息队列集成等基础设施已完成骨架搭建。

---

## 2. 总体架构设计

### 2.1 架构设计原则

1. **响应式优先**：全链路基于 Reactor 的 `Mono`/`Flux`，避免阻塞调用
2. **策略模式**：过滤器编排、负载均衡、限流算法、健康检查策略均采用策略模式实现可插拔
3. **反射插件注册**：注册中心客户端、流量控制操作符通过枚举 + 反射实例化，新增类型只需添加枚举常量
4. **事件驱动**：缓存刷新、日志发布、服务发现变更通过 Spring `ApplicationEvent` 解耦
5. **读写锁分离**：过滤器链构建（写）与请求执行（读）分离，保证热刷新期间的线程安全
6. **配置外置化**：运行时配置集中托管于 Nacos，应用本身仅保留端口、Actuator 等静态配置

### 2.2 总体架构分层图

```
┌─────────────────────────────────────────────────────────────────────┐
│                        接入层 (Access Layer)                        │
│   HTTP / WebSocket / SMPP / AI / Admin / Backend 多协议路由入口       │
│                     AirGatewayEndpointConfig                         │
└──────────────────────────────┬──────────────────────────────────────┘
                               │
┌──────────────────────────────▼──────────────────────────────────────┐
│                     网关引擎层 (Gateway Engine)                      │
│                        AirGatewayEngine                             │
│           implements { routeApi / routeAdmin / routeAi /            │
│       routeWebsocket / routeSmpp / forwardToBackend }                │
└──────────────────────────────┬──────────────────────────────────────┘
                               │
┌──────────────────────────────▼──────────────────────────────────────┐
│                   全局过滤器层 (Global WebFilter)                     │
│   AirGatewayLogFilter (HIGHEST_PRECEDENCE-1) — 访问/错误日志         │
│   AirGatewayMetricsFilter — 请求指标采集                             │
└──────────────────────────────┬──────────────────────────────────────┘
                               │
┌──────────────────────────────▼──────────────────────────────────────┐
│                自定义过滤器引擎 (Custom Filter Engine)                 │
│            CustomerAirGatewayFilterManager (ReadWriteLock)           │
│     ┌──────────────────┐   ┌──────────────────┐                     │
│     │ AirGatewayStrategy│   │CustomWebFilterChain│ ← 每次请求新实例  │
│     │ (filter排序列表)   │   │ (AtomicInteger走链) │                   │
│     └──────────────────┘   └──────────────────┘                     │
│         AbstractAirGatewayFilter (enable/disable 门控)               │
└──────────────────────────────┬──────────────────────────────────────┘
                               │
┌──────────────────────────────▼──────────────────────────────────────┐
│              流量控制管道 (Flow Control Pipeline)                      │
│              AirFlowControlPipelineBuilder                           │
│   rateLimit(Resilience4j│TokenBucket│SlidingWindow)                │
│   circuitBreaker(Resilience4j)                                      │
│   degradeOnCondition (待实现)                                         │
└──────────────────────────────┬──────────────────────────────────────┘
                               │
┌──────────────────────────────┴──────────────────────────────────────┐
│                                          │                          │
│   ┌──────────────────────┐    ┌──────────▼──────────┐               │
│   │   负载均衡 (LB)       │    │   路由定位 (Router)   │              │
│   │ AirReactiveLoadBalance│    │AirMultiRegistryRoute  │             │
│   │ round-robin / random │    │DefinitionLocator      │             │
│   │ / weighted           │    │(动态服务→路由定义)      │             │
│   └──────────┬───────────┘    └───────────────────────┘             │
│              │                                                       │
│   ┌──────────▼───────────┐    ┌───────────────────────┐              │
│   │ 服务发现 (Discovery)  │    │   RPC 客户端层         │              │
│   │ 多注册中心管理器       │    │ Http / gRPC / RSocket │              │
│   │ (Nacos/ZK/Consul/Eureka)│   │ AirGatewayRPCService │             │
│   └──────────────────────┘    └───────────────────────┘              │
└─────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────┐
│                      横切关注点 (Cross-Cutting)                      │
│  缓存(Caffeine/Redis/MongoDB)  日志(LogPublisher/LogStorage)        │
│  监控(Micrometer/Prometheus)   健康检查(Instance/Registry)          │
│  事件分发(AirGatewayEventDispatcher) 配置管理(Nacos热刷新)          │
│  消息队列(Kafka/RocketMQ)      Seata分布式事务                      │
└─────────────────────────────────────────────────────────────────────┘
```

### 2.3 技术选型

| 技术领域 | 选型 | 版本 | 说明 |
|---------|------|------|------|
| 核心框架 | Spring Boot | 3.2.12 | 基础运行框架 |
| 响应式 Web | Spring WebFlux | 随 Boot | 全链路响应式 HTTP 处理 |
| Spring Cloud | spring-cloud-dependencies | 2023.0.3 | 云原生组件 BOM |
| 阿里生态 | spring-cloud-alibaba-dependencies | 2023.0.3.2 | Nacos / Seata / RocketMQ |
| 服务网关 | spring-cloud-gateway-starter | 随 Cloud | 网关基础（部分自动配置被禁用） |
| 注册中心 | Nacos / ZooKeeper / Consul / Eureka | - | 多注册中心并发支持 |
| 配置中心 | Nacos Config | - | 运行时配置托管 |
| 缓存 | Caffeine + Redisson + MongoDB | Redisson 3.27.1 | 多级缓存 |
| RPC | RSocket / gRPC / HTTP | grpc-starter 3.1.0 | 多协议后端调用 |
| 消息队列 | Kafka + RocketMQ | Kafka 3.2.3 | 异步消息 |
| 熔断限流 | Resilience4j | 随 circuitbreaker | 响应式熔断器/限流 |
| 可观测性 | Micrometer + Prometheus | - | 指标采集 |
| 日志 | SLF4J + Lombok | - | 自定义异步日志体系 |
| 分布式事务 | Seata | - | 分布式事务（配置已就位） |
| 时钟抽象 | 自定义 `AirGatewayJavaClock` | - | 统一时间源便于测试 |

---

## 3. 核心模块设计

### 3.1 接入层 (Access Layer)

**所在包**：`com.airfree.access`

| 类 | 职责 |
|----|------|
| `AirGatewayEndpointConfig` | 定义唯一的 WebFlux `RouterFunction` Bean，将 6 类路径断言映射到网关引擎方法 |

**路由映射表**：

| 路径前缀 | 处理器 | 用途 |
|---------|--------|------|
| `/api/**` | `routeApiRequest` | 业务 API 请求 |
| `/admin/**` | `routeAdminRequest` | 管理后台请求 |
| `/ai/**` | `routeAiRequest` | AI 服务请求 |
| `/websocket/**` | `routeWebsocketRequest` | WebSocket 请求 |
| `/smpp/**` | `routeSmppRequest` | SMPP 短信请求 |
| `/backend/**` | `forwardToBackend` | 后端服务转发 |

该 Bean 标记 `@Order(HIGHEST_PRECEDENCE)` 确保路由注册优先级最高。路由函数外层统一 Wrapper 了一个 filter 用于记录路由匹配/成功/失败日志。

---

### 3.2 网关引擎层 (Gateway Engine)

**所在包**：`com.airfree.core`

| 类 | 类型 | 职责 |
|----|------|------|
| `AirGateway` | 接口 | 定义 6 个路由方法 + 1 个转发方法 |
| `AirGatewayEngine` | 实现 | 当前各方法调用过滤器链后返回 mock 响应 |
| `AirGatewayRouteLocator` | 桩接口 | 预留 |
| `AirGatewayLoadBalancer` | 桩接口 | 预留 |
| `AirGatewayAccessAdapter` | 桩接口 | 预留 |

**设计要点**：引擎与转发逻辑解耦。`AirGatewayEngine` 实现 `AirGateway` 接口定义的路由契约，但实际转发至后端服务由 RPC 客户端层承担，引擎层当前作为编排入口使用。

---

### 3.3 自定义过滤器引擎 (Custom Filter Engine)

**所在包**：`com.airfree.filter`

#### 3.3.1 核心接口与类

| 类 | 类型 | 职责 |
|----|------|------|
| `AirGatewayFilterManager` | 接口 | 定义过滤器链构建、执行、策略查找三个方法 |
| `CustomerAirGatewayFilterManager` | 实现 | 启动时扫描所有 `AbstractAirGatewayFilter` 和 `AirGatewayStrategy` bean → 构建 `Map<策略, 过滤器列表>` |
| `AirGatewayStrategy` | 接口 | 策略接口：返回过滤器 bean 名称的有序列表 |
| `ApiFilterStrategy` | 实现 | 目前唯一的策略实现 |
| `AbstractAirGatewayFilter` | 抽象基类 | 实现 `WebFilter`，提供 `enable/disable` 门控 |
| `CustomWebFilterChain` | 链实现 | **每次请求新建实例**，`AtomicInteger` 索引顺序执行 |
| `ProtocolApiEnum` | 枚举 | REST / SOAP / 不支持类型 三种协议标识 |

#### 3.3.2 过滤器执行流程

```
请求到达
  → CustomerAirGatewayFilterManager.executeFilterChain(strategy, exchange, chain)
    → 读锁内：取 customerFiltersChainMap.get(strategy) → 过滤掉 isDisable() 的 filter
    → new CustomWebFilterChain(enabledFilters, originalChain) — 每次请求新建
    → customChain.filter(exchange)
      → index.getAndIncrement() → filter.filter(exchange, this) → ... 
      → 所有 filter 执行完毕 → originalChain.filter(exchange) (回退到 Spring 原生链)
```

#### 3.3.3 线程安全设计

- **ReadWriteLock**：`buildAllFilterChainByStrategy()` 在写锁下原子替换整个映射；请求执行在读锁下获取过滤器列表
- **CopyOnWriteArrayList**：策略内的过滤器顺序列表使用 `CopyOnWriteArrayList`
- **CustomWebFilterChain 每次请求新建**：`AtomicInteger` 索引是有状态实例字段，不可共享

#### 3.3.4 已有全局过滤器 (不受策略系统管理)

| 过滤器 | 执行顺序 | 职责 |
|--------|---------|------|
| `AirGatewayLogFilter` | `HIGHEST_PRECEDENCE - 1` | 最外层，记录请求入/出日志、响应状态、耗时、错误堆栈 |
| `AirGatewayMetricsFilter` | - | 指标采集 |

#### 3.3.5 已有自定义过滤器

| 过滤器 | 职责 |
|--------|------|
| `ApiInternalRequestConvertFilter` | 根据 Content-Type 识别协议类型(SOAP/REST) → 写入 exchange attributes → 转换请求体 → 重构 request |

---

### 3.4 流量控制管道 (Flow Control Pipeline)

**所在包**：`com.airfree.flow`

#### 3.4.1 核心类

| 类 | 职责 |
|----|------|
| `AirFlowControlOperator<T>` | 操作符接口，extends `Function<Mono<T>, Mono<T>>` |
| `AirFlowControlPipelineBuilder` | 从配置读取 pipeline → 按 orderId 排序 → 链式构建 `transform` |
| `AirFlowControlOperatorFactory` | 通过反射从 `AirFlowControlType` 枚举实例化操作符 |
| `AirFlowControlConfigProperties` | `@ConfigurationProperties("flow-control")`，绑定 pipelines 映射 |
| `AirOperatorConfig` | 单个操作符配置：operator / algorithm / resource / config(Map) / orderId |

#### 3.4.2 支持的算法类型 (`AirFlowControlType`)

| 操作类型 | 算法 | 实现类 | 状态 |
|---------|------|--------|------|
| rateLimit | Resilience4j | `Resilience4jRateLimitOperator` | 配置已完整，apply 返回 null（待接通） |
| rateLimit | TokenBucket | `AirTokenBucketRateLimitOperator` | 完整实现(令牌桶算法) |
| rateLimit | SlidingWindow | `AirSlidingWindowRateLimitOperator` | 文件存在 |
| circuitBreaker | Resilience4j | `Resilience4jCircuitBreakerOperator` | 完整实现(熔断状态机) |
| degradeOnCondition | Resilience4j | `AirGeneralDegradeOnConditionOperator` | 仅类壳，待实现 |

#### 3.4.3 管道构建流程

```
buildPipeline("order-process", resources)
  → 读取 flow-control.pipelines["order-process"]
  → sortOperatorChain (按 orderId 升序)
  → 遍历每个 AirOperatorConfig：
    → AirFlowControlType.getByFlowTypeAndAlgorithm(type, algorithm)
    → AirFlowControlOperatorFactory.getAirFlowControlOperator(type, config, fallback)
    → 反射调用构造器：(String, AirFlowControlType, AirOperatorConfig, Function)
  → 返回 Function<Mono<T>, Mono<T>> 链（每个 operator 通过 result.transform(operator) 串联）
```

#### 3.4.4 配套异常体系

| 异常类 | 触发场景 |
|--------|---------|
| `AirCircuitBreakerOpenException` | 熔断器打开时请求被拒绝 |
| `AirRateLimitExceededException` | 限流阈值被突破 |
| `AirServiceUnavailableException` | 后端服务不可用 |

---

### 3.5 服务发现 (Service Discovery)

**所在包**：`com.airfree.discovery`

#### 3.5.1 核心类

| 类 | 职责 |
|----|------|
| `AirReactiveServiceDiscovery` | 发现客户端接口：初始化 / 获取实例 / 订阅变化 / 健康检查 / 注册 / 注销 |
| `AirServiceDiscoveryTypeEnum` | 枚举：nacos→AirNacosReactiveDiscovery / zookeeper→... / consul→... / eureka→... |
| `AirReactiveRegistryCenterFactory` | 反射构造 + 初始化注册中心客户端（`computeIfAbsent` 缓存） |
| `AirReactiveServiceDiscoveryManager` | **核心管理器**，管理所有注册中心客户端，提供统一发现 API |
| `MultiAirRegistryCenterConfig` | 多注册中心配置绑定（`discovery.registries.*`） |
| `AirRegistryCenterConfig` | 单个注册中心配置 |
| `AirServiceInstance` | 服务实例领域对象（含双维度健康状态） |
| `AirServiceDiscoveryCache` | 基于 Caffeine 的实例缓存（30s TTL, 1000 容量） |

#### 3.5.2 服务实例领域模型 (`AirServiceInstance`)

```
AirServiceInstance
  ├── instanceId / serviceId / host / port / secure / uri / metadata
  ├── registryHealthy  (注册中心报告的健康状态 — volatile)
  ├── clientHealthy    (客户端主动检查的健康状态 — volatile)
  ├── lastHealthCheckTime
  ├── inMaintenance
  ├── isHealthy()        → registryHealthy && clientHealthy && !inMaintenance
  └── isMetadataHealthy() → 检查 metadata 中的 maintenance/out_of_service/registry.status
```

#### 3.5.3 多注册中心管理机制

```
AirReactiveServiceDiscoveryManager (构造方法)
  → initializationMono (异步)
    → validateConfig() → 遍历 multiRegistryConfig.getConfigs()
      → registryFactory.createDiscovery(config) → 反射构造 → discovery.initialize(config)
    → registerAirGatewayToRegistries() → 将自身("air-gateway")注册到所有支持注册的客户端
  → 后续所有公共方法先 ensureInitialized() 阻塞等待初始化完成
```

#### 3.5.4 服务发现公共 API

| 方法 | 说明 |
|------|------|
| `discoverService(serviceId)` | 从所有注册中心获取实例（带缓存） |
| `getHealthyInstances(serviceId)` | 过滤健康实例（默认策略） |
| `getHealthyInstances(serviceId, strategy)` | 按指定策略过滤 |
| `getAvailableInstances` / `getLenientHealthyInstances` / `getHybridHealthyInstances` | 预置策略快捷方法 |
| `getAllServiceNames()` | 获取所有服务名 |
| `subscribeService(serviceId)` | 订阅实例变化 |
| `getInstancesByVersion` / `getInstancesByZone` | 按元数据过滤 |
| `refreshCache` / `refreshCacheWithRetry` / `refreshMultipleCaches` | 缓存刷新 |
| `getRegistryHealth` / `getRegistryHealthStats` / `getOverallRegistryHealth` | 注册中心健康状态 |

#### 3.5.5 注册中心健康检查

`AirRegistryCenterHealthService` 对注册中心客户端本身进行健康检查：
- 调用 `discovery.isHealthy()` + 超时控制
- 结果缓存 5 分钟
- 支持定时订阅 `subscribeHealthChanges`（基于 `Flux.interval`）

#### 3.5.6 事件体系

| 事件 | 触发时机 |
|------|---------|
| `AirServiceDiscoveryEvent` | 服务发现完成（携带实例列表） |
| `AirServiceInstanceEvent` | 实例变更（ADDED / REMOVED / MODIFIED） |
| `AirRegistryCenterHealthEvent` | 注册中心健康状态变化 |

---

### 3.6 负载均衡 (Load Balancer)

**所在包**：`com.airfree.LoadBalance`

| 类 | 职责 |
|----|------|
| `AirLoadBalanceStrategy` | 策略接口：`choose(instances, exchange) → Mono<AirServiceInstance>` |
| `AirReactiveLoadBalancer` | 从 discoveryManager 获取健康实例 → 委托策略选择 |
| `AirRoundRobinStrategy` | 轮询（AtomicLong 计数器取模） |
| `AirRandomStrategy` | 随机 |
| `AirWeightedRoundRobinStrategy` | 加权轮询 |

策略在 `AirReactiveLoadBalancer` 构造时注册到 `ConcurrentHashMap<String, AirLoadBalanceStrategy>`，默认策略为 `round-robin`。`strategy/customerImpl/` 包预留为用户自定义策略扩展点。

---

### 3.7 路由子系统 (Router)

**所在包**：`com.airfree.router`

| 类 | 职责 |
|----|------|
| `AirMultiRegistryRouteDefinitionLocator` | 实现 Spring Cloud Gateway 的 `RouteDefinitionLocator` 接口 |
| `AirRouteMatcher` | 带缓存的路由断言匹配器（Path / Host / Method） |

**`AirMultiRegistryRouteDefinitionLocator` 工作机制**：

1. **启动时**：`discoveryManager.getAllServiceNames()` → 为每个服务创建路由定义
2. **动态监听**：`subscribeService(serviceId)` → 实例变化时更新/删除路由
3. **路由类型**：默认路由(`/serviceId/**` + 路径重写) + 自定义路径路由 + 版本路由 + 权重路由
4. **RPC 增强**：`enhanceRouteWithRPCInfo()` 从实例元数据提取 `rpc.protocol` / `rpc.serialization` / `rpc.timeout` 写入路由 metadata
5. **URI 格式**：`lb://serviceId`（走 Spring Cloud 负载均衡）

---

### 3.8 RPC 客户端层 (RPC Client Layer)

**所在包**：`com.airfree.rpc`

| 类 | 职责 |
|----|------|
| `AirGatewayRPCServiceClient` | 接口：`execute` / `executeStream` / `support(endpoint)` / `getProtocol()` |
| `HttpServiceClient` | HTTP 协议客户端（`WebClient.Builder` 注入，方法体为 null — 待接通） |
| `GRPCServiceClient` | gRPC 协议客户端（`ManagedChannel` 连接池，方法体为 null — 待接通） |
| `RSocketServiceClient` | RSocket 协议客户端（`RSocketRequester` 连接池，方法体为 null — 待接通） |
| `AirRPCTypeEnum` | 枚举：RSOCKET / GRPC / HTTP |
| `AirRpcRequestContext` | 请求上下文：serviceName / methodPath / httpMethod / headers / body / endpoint |
| `AirServiceEndpoint` | 端点模型：serviceName / protocol / endpoint / metadata |
| `AirRPCResponse<T>` | 响应包装（当前为空类） |

**请求构建器**（待完善）：`HttpRequestBuilder`（实现 `WebClient.Builder`）、`RSocketRequestBuilder`（实现 `RSocketRequester.Builder`）——方法体均为 null，为后续协议适配预留。

---

### 3.9 消息队列 (MQ)

**所在包**：`com.airfree.mq`

#### 3.9.1 生产者

| 工厂类 | 职责 |
|--------|------|
| `AirKafkaBatchSenderFactory` | 批量创建 `KafkaSender`（Reactor Kafka），构造时遍历 `kafka.producers` 配置 |
| `AirRocketMqBatchProducerFactory` | 批量创建 `RocketMQTemplate`，构造时遍历 `rocketmq.producers` 配置 |

#### 3.9.2 消费者

| 工厂类 | 职责 |
|--------|------|
| `AirKafkaBatchReceiverFactory` | 批量创建 `KafkaReceiver` → 通过类名反射实例化 `AirReactiveKafkaListener` → 绑定 `receive().concatMap(listener.onMessage)` |
| `AirRocketMqBatchConsumerFactory` | 批量创建 `DefaultMQPushConsumer` → 反射实例化 `MessageListenerConcurrently/Orderly` → 启动消费 |

#### 3.9.3 配置模型

| 配置前缀 | 绑定点 | 结构 |
|---------|--------|------|
| `kafka.*` | `AirKafkaConfigProperties` | `Map<String, ProducerConfig>` + `Map<String, ConsumerConfig>` |
| `rocketmq.*` | `AirRocketMQConfigProperties` | `nameServer` + producers/consumers Map |

**Kafka Consumer 特色**：支持一类型多实例 (`instanceNums`)，实例名带时间戳保证唯一。

**RocketMQ Consumer 特色**：消费线程池 5-10，fetch 路由间隔 10s，消费超时 15 分钟，实例稳定性优化。

#### 3.9.4 索引

- `ConsumerIndex`：`(ConsumerConfig, index)` 二元组作为消费者容器 key，提供同类型/同实例判断
- `ProducerIndex`：`(ProducerConfig, index)` 二元组

---

### 3.10 缓存子系统 (Cache Subsystem)

**所在包**：`com.airfree.cache`

#### 3.10.1 多级缓存架构

| 存储 | 客户端类 | 配置类 | 用途 |
|------|---------|--------|------|
| Caffeine (本地) | 内嵌于 `AirServiceDiscoveryCache` | - | 服务实例列表缓存 |
| Redis (Redisson) | `AirRedissonClient` | `AirRedissonConfigProperties` + `AirRedissonConfigFactory` | 分布式缓存 |
| MongoDB | `AirMongoDbClient` + `ReactiveMongoTemplate` | `AirMongoDbConfigProperties` | 文档型缓存/持久化缓存 |

#### 3.10.2 缓存配置

**Redisson**：通过 YAML 字符串配置（`singleConfig` / `clusterConfig`），`mode` 字段切换单机/集群，`Config.fromYAML()` 解析。

**MongoDB**：精细化连接池 / Socket / Server / SSL / 压缩器配置。`MongoClientSettings.Builder` 构建，支持 Snappy + Zlib 压缩。

#### 3.10.3 缓存事件体系

| 枚举值 | 依赖基底 | 操作类型 |
|--------|---------|---------|
| `DEPEND_BASE_LOCAL_ADD/UPDATE/DELETED` | local | add/update/deleted |
| `DEPEND_BASE_REDIS_ADD/UPDATE/DELETED` | redis | add/update/deleted |

**事件流转**：
```
缓存变更 → AirGatewayAbstractCacheEvent (携带 operation/key/value/reason)
  → ConfigFlushEventPublisher.syncPublishEvent / asyncPublishEvent
    → ApplicationEventPublisher
      → AirGatewayEventDispatcher.onApplicationEvent
        → 按 dependBase 分派：local → listener.onLocalReFlush; redis → listener.onRedisReFlush
```

#### 3.10.4 缓存实体

`AirCacheEntity`：cacheId / baseOnDatabaseType(REDIS/MONGODB/CASSANDRA) / key / value / cacheDataType(STRING/HASH/LIST/SET/ZSET/DOCUMENT) / cacheBizType

---

### 3.11 日志子系统 (Log Subsystem)

**所在包**：`com.airfree.log`

#### 3.11.1 核心类

| 类 | 职责 |
|----|------|
| `AirGatewayLogProperties` | `@ConfigurationProperties("air-gateway.log")`：级别 / 存储类型 / 路径 / 开关 / 线程池 / 队列容量 / 头过滤 / 忽略路径 |
| `AirGatewayLogPublisher` | 日志发布器：异步调度器 (`Schedulers.newBoundedElastic`)，发布访问/错误/业务日志 |
| `AirGatewayLogContext` | 在 `ServerWebExchange` 上管理 traceId / spanId / requestId |
| `AirGatewayLogAnnotation` | 声明式方法日志注解（level / type / async） |
| `AirGatewayLogAspect` | AspectJ 切面：`@Around("@annotation")` 拦截，根据返回类型 (Mono/Flux/同步) 分别处理 |
| `AirGatewayLogStorage` | 存储接口：saveAccessLog / saveErrorLog / saveBusinessLog / queryAccessLogs / queryErrorLogs |
| `DefaultAirGatewayLogStorage` | 默认实现 |

#### 3.11.2 日志分类

| 日志类型 | 发布方式 | 事件 |
|---------|---------|------|
| 访问日志 | `publishAccessLog()` | `AirGatewayLogEvent` |
| 错误日志 | `publishErrorLog()` | `AirGatewayErrorEvent` |
| 业务日志 | `publishBusinessLog()` (支持注解 `@AirGatewayLogAnnotation`) | 无事件 |

#### 3.11.3 切面处理策略

`AirGatewayLogAspect.aroundAnnotatedMethod()` 通过 `MethodSignature.getReturnType()` 判断：
- `Mono.class.isAssignableFrom(returnType)` → `handleMonoMethod` → `Mono.defer` + `doOnSuccess/doOnError`
- `Flux.class.isAssignableFrom(...)` → `handleFluxMethod` → `Flux.defer` + `doOnComplete/doOnError`
- 其他 → `handleSyncMethod` → `joinPoint.proceed()` 同步执行

#### 3.11.4 日志实体

`AirGatewayLog`：traceId / spanId / requestId + method / path / queryParams / headers / clientIp + statusCode / responseTime / costTime / errorMessage / errorStack + serviceId / routeId / targetUrl + instanceId / hostname / timestamp + extraInfo

#### 3.11.5 自动配置

`AirGatewayLogAutoConfiguration` 当前**全部注释掉**（`AirGatewayLogFilter`、`AirGatewayLogPublisher`、`AirGatewayLogStorage` 的 Bean 定义被注释），日志组件通过 `@Component` 注解直接注册。

---

### 3.12 健康检查 (Health Check)

**所在包**：`com.airfree.health`

#### 3.12.1 核心类

| 类 | 职责 |
|----|------|
| `AirServiceInstanceHealthCheckService` | 服务实例级健康检查，4 种策略 |
| `AirRegistryCenterHealthService` | 注册中心客户端级健康检查 |
| `AirServiceInstanceHealthCheckStrategy` | 策略枚举：REGISTRY_BASED / CLIENT_ACTIVE / HYBRID / LENIENT / METADATA_BASED |
| `AirServiceInstanceHealthCheckResult` | 检查结果：healthy + timestamp |

#### 3.12.2 实例健康检查策略

| 策略 | 逻辑 |
|------|------|
| `REGISTRY_BASED` | 仅看 `instance.isMetadataHealthy()` |
| `CLIENT_ACTIVE` | TCP Socket 连接检查 (host:port)，结果缓存 |
| `HYBRID` | 注册中心状态 **AND** 客户端检查都健康 |
| `LENIENT` | 注册中心 OR 客户端 OR 可恢复(metadata 中未超维护窗口 + 权重>50) |
| `METADATA_BASED` | 枚举定义但 switch 未实现（走 default） |

**降级处理**：当 `config.isEnableFallback()` 开启且无健康实例时，`getFallbackInstances()` 返回降级列表（当前返回空列表，待实现）。

#### 3.12.3 注册中心健康检查

`AirRegistryCenterHealthService`：
- 调用 `discovery.isHealthy()` + `config.getCheckTimeout()` 超时
- 缓存 5 分钟
- 支持并发控制 (`config.getMaxConcurrentChecks()`)
- `subscribeHealthChanges()` 基于 `Flux.interval` 定时检查 + `distinctUntilChanged` 过滤重复

#### 3.12.4 健康配置

| 配置类 | 前缀 | 内容 |
|--------|------|------|
| `AirServiceInstanceHealthCheckConfigProperties` | - | defaultStrategy / clientCheckPort / clientCheckTimeout / cacheDuration / enableFallback / lenientMaxUnhealthyMinutes |
| `AirRegistryCenterHealthConfigProperties` | - | checkTimeout / maxConcurrentChecks / healthCheckInterval / enableDetailedLogging |

---

### 3.13 监控子系统 (Monitor Subsystem)

**所在包**：`com.airfree.monitor`

| 类 | 职责 |
|----|------|
| `AirGatewayJavaClock` | 自定义 `java.time.Clock` 实现，统一时源（便于测试） |
| `AirGatewayActuatorClock` | 另一个时钟实现 |
| `AirGatewayMetricsConfig` | 注册 `TimedAspect` / `CountedAspect`；通用标签 (application/环境/实例) |
| `AirGatewayMetricsCollector` | 核心指标采集器 |
| `AirGatewayCustomMetricsEndpoint` | Actuator 自定义端点 `@Endpoint(id="custom")` |

#### 3.13.1 采集指标

| 指标名 | 类型 | 说明 |
|--------|------|------|
| `gateway.requests.total` | Counter | 总请求数 |
| `gateway.requests.error` | Counter | 错误请求数 |
| `gateway.request.duration` | Timer | 请求耗时(P50/P95/P99 + 直方图) |
| `gateway.request.size` | DistributionSummary | 请求大小(bytes) |
| `gateway.requests.active` | Gauge | 当前活跃请求数 |
| `gateway.requests.by.route` | Counter | 按路由请求数 |
| `gateway.request.duration.by.route` | Timer | 按路由请求耗时 |
| `gateway.requests.error.by.status` | Counter | 按状态码错误数 |
| `gateway.requests.by.status` | Counter | 按状态码请求数 |
| `gateway.route.latency` | Timer | 路由级别延迟 |
| `gateway.backend.call.duration` | Timer | 后端调用耗时 |
| `gateway.backend.calls` | Counter | 后端调用次数 |
| `gateway.requests.total` (LogPublisher) | Counter | 日志请求计数 |
| `gateway.request.duration` (LogPublisher) | Timer | 日志请求耗时 |
| `gateway.requests.error` (LogPublisher) | Counter | 日志错误计数 |

#### 3.13.2 JVM / 系统指标

通过 `JvmMemoryMetrics` / `JvmGcMetrics` / `ProcessorMetrics` / `LogbackMetrics` 自动绑定。

#### 3.13.3 Actuator 端点

`application.yml` 暴露：`health, metrics, prometheus, info`。自定义端点 `/actuator/custom` 返回 totalRequests / errorRate / avgResponseTime / p95ResponseTime / maxResponseTime。

---

### 3.14 配置管理 (Config Management)

#### 3.14.1 配置分层

| 层次 | 来源 | 内容 |
|------|------|------|
| 静态配置 | `application.yml`（仓库内） | 端口 10101、日志级别、Actuator 端点、`air-gateway.log.*` 队列容量 |
| 动态配置 | Nacos（`bootstrap.yml` 导入） | 注册中心地址、Redis、MongoDB、流量控制管道、RocketMQ、Kafka、Seata |

#### 3.14.2 Nacos 配置导入 (`bootstrap.yml`)

通过 `spring.config.import` 导入 6 个 Nacos data-id：
1. `airRocketMQConfigProperties.yaml` — RocketMQ
2. `airRedissonConfig.yaml` — Redis/Redisson
3. `airMongoDbConfig.yaml` — MongoDB
4. `airFlowControl.yaml` — 流量控制管道
5. `airKafkaConfigProperties.yaml` — Kafka
6. `airDiscoveryConfig.yaml` — 服务发现注册中心

所有配置均带 `refresh: true` 支持热刷新。

#### 3.14.3 冲突禁用项

`bootstrap.yml` 显式禁用与自定义实现冲突的 Spring Cloud 自动配置：

| 禁用项 | 原因 |
|--------|------|
| `spring.cloud.service-registry.auto-registration.enabled=false` | 自定义服务自注册逻辑 |
| `spring.cloud.gateway.discovery.locator.enabled=false` | 使用 `AirMultiRegistryRouteDefinitionLocator` 替代 |
| `spring.cloud.consul.enabled=false` | 按需启用 |
| `spring.cloud.gateway.filter.json-to-grpc.enabled=false` | 避免 gRPC 自动配置启动报错 |

#### 3.14.4 Nacos 配置变更监听

**`AirNacosConfigChangeListenerManager`**：
- 启动时遍历 `NacosConfigChangeElementType` 枚举 → 通过 `NacosConfigManager.getConfigService().addListener(dataId, group, listener)` 注册监听

**`RocketMqConfigChangeListener`**（已有实现）：
- 接收 `ConfigChangeEvent` → 过滤非增量的变更类型（DELETED/MODIFIED 被拒绝）
- 解析 field key → 构建 `ProducerConfig` / `ConsumerConfig` → 调用工厂重建 producer/consumer 实例

---

### 3.15 事件分发 (Event Dispatcher)

**所在包**：`com.airfree.listener`

| 类 | 职责 |
|----|------|
| `AirGatewayEventDispatcher` | 唯一 `ApplicationListener<ApplicationEvent>`，按事件类型分派 |
| `AirGatewayListener<T>` | 监听器接口（onCacheRefresh / onCacheEvict / onCacheLoad / supports / getOrder） |

**分派逻辑**：
- `AirGatewayAbstractCacheEvent` → 遍历 `cacheEventListenerList`（按 order 排序）→ 反射分派 `onLocalReFlush` / `onRedisReFlush`
- `AirGatewayAbstractLogEvent` → 遍历 `logEventListenerList` → 调用 `onLogRecordFish`

---

## 4. 数据流设计

### 4.1 请求处理主链路

```
外部请求 (HTTP/WebSocket/SMPP...)
  │
  ▼
AirGatewayEndpointConfig (RouterFunction, @Order(HIGHEST_PRECEDENCE))
  │  路径匹配: /api/** /admin/** /ai/** /websocket/** /smpp/** /backend/**
  ▼
┌─ 全局 WebFilter 链 ─────────────────────────────────────────┐
│  AirGatewayLogFilter (HIGHEST_PRECEDENCE-1)                  │
│    → AirGatewayLogContext.initLogContext(exchange)           │
│    → 记录请求 → chain.filter → 记录响应/错误                  │
│  AirGatewayMetricsFilter                                     │
└─────────────────────────────────────────────────────────────┘
  │
  ▼
AirGatewayEngine.routeXxxRequest(serverRequest)
  │  customerAirGatewayFilterManager.findAirGatewayStrategyName("ApiFilterStrategy")
  │  customerAirGatewayFilterManager.executeFilterChain(strategy, exchange, null)
  ▼
CustomWebFilterChain (per-request new instance)
  │  [ApiInternalRequestConvertFilter → ...其他 filter]
  ▼
┌─ 流量控制管道(接通后) ──────────────────────────────────────┐
│  rateLimit → degradeOnCondition → circuitBreaker             │
│  (每个 operator 通过 mono.transform(operator) 串联)          │
└─────────────────────────────────────────────────────────────┘
  │
  ▼
┌─ 负载均衡 + RPC(接通后) ───────────────────────────────────┐
│  discoveryManager.getHealthyInstances(serviceId)             │
│    → AirReactiveLoadBalancer.choose(serviceId, strategy, exch)│
│      → AirLoadBalanceStrategy.choose(instances, exchange)   │
│    → RPC client.execute(context)                            │
└─────────────────────────────────────────────────────────────┘
  │
  ▼
ServerResponse (mock / 真实后端响应)
```

### 4.2 服务发现数据流

```
启动阶段
  MultiAirRegistryCenterConfig (Nacos airDiscoveryConfig.yaml)
    → AirReactiveServiceDiscoveryManager.initialize()
      → registryFactory.createDiscovery(config) [反射]
        → AirServiceDiscoveryTypeEnum → 对应实现类 newInstance()
        → discovery.initialize(config)
    → registerAirGatewayToRegistries()
      → discovery.registerInstance("air-gateway", host, port, metadata)

运行时
  请求 → AirReactiveServiceDiscoveryManager.discoverService(serviceId)
    → AirServiceDiscoveryCache.getOrLoad(serviceId, loader)
      → Caffeine 缓存命中 → 返回
      → 未命中 → fetchInstancesFromAllRegistries(serviceId)
        → 遍历所有注册中心 getInstances() → distinct(instanceId) → 缓存

  服务变化 → subscribeService(serviceId)
    → 各注册中心 subscribe(serviceId) → AirServiceInstanceEvent (ADDED/REMOVED/MODIFIED)
      → AirMultiRegistryRouteDefinitionLocator.handleServiceInstanceEvent()
        → 更新/删除 RouteDefinition

  注册中心健康 Flux.interval → checkRegistryHealth() → AirRegistryCenterHealthEvent
```

### 4.3 缓存刷新数据流

```
缓存配置更新 (Nacos configChangeEvent)
  → ConfigFlushEventPublisher.syncPublishEvent / asyncPublishEvent(@Async)
    → ApplicationEventPublisher.publishEvent(AirGatewayAbstractCacheEvent)
      → AirGatewayEventDispatcher.onApplicationEvent()
        → 判断 dependBase：
          "local" → listener.onLocalReFlush(event)
          "redis" → listener.onRedisReFlush(event)
        → 各 AirGatewayCacheEventListener 按 order 排序执行
```

### 4.4 日志数据流

```
请求层：AirGatewayLogFilter
  → AirGatewayLogContext.initLogContext()  [生成 traceId/spanId/requestId]
  → logPublisher.publishAccessLog(gatewayLog) / publishErrorLog()
    → Schedulers.newBoundedElastic("gateway-log") 异步执行
      → recordMetrics() / logStorage.saveXxxLog() / eventPublisher.publishEvent()

注解层：@AirGatewayLogAnnotation
  → AirGatewayLogAspect.aroundAnnotatedMethod() [@Around]
    → 按返回类型 (Mono/Flux/同步) 处理
    → logPublisher.publishBusinessLog(traceId, className, methodName, data)
```

---

## 5. 设计模式应用

| 模式 | 应用位置 | 实现方式 |
|------|---------|---------|
| **策略模式** | 过滤器编排 (`AirGatewayStrategy`) | 策略定义 filter bean 名称有序列表 |
| **策略模式** | 负载均衡 (`AirLoadBalanceStrategy`) | round-robin / random / weighted 可替换 |
| **策略模式** | 健康检查 (`AirServiceInstanceHealthCheckStrategy`) | REGISTRY_BASED / CLIENT_ACTIVE / HYBRID / LENIENT |
| **工厂+反射** | 注册中心客户端 (`AirReactiveRegistryCenterFactory`) | 枚举 `Class` 字段 → `Constructor.newInstance()` |
| **工厂+反射** | 流量控制操作符 (`AirFlowControlOperatorFactory`) | 枚举 `Class` 字段 → 反射匹配构造器 |
| **模板方法** | 过滤器基类 (`AbstractAirGatewayFilter`) | `enable/disable/isDisable` 门控模板 |
| **责任链** | 过滤器链 (`CustomWebFilterChain`) | `AtomicInteger` 索引顺序执行 |
| **管道-过滤器** | 流量控制 (`AirFlowControlPipelineBuilder`) | `Function<Mono,Mono>` 链式 transform |
| **观察者/事件** | 缓存/日志事件分发 (`AirGatewayEventDispatcher`) | Spring `ApplicationEvent` + `ApplicationListener` |
| **门面** | 服务发现管理器 (`AirReactiveServiceDiscoveryManager`) | 统一封装多注册中心操作 |
| **Builder** | 日志实体 (`AirGatewayLog.@Builder`)、缓存实体 (`AirCacheEntity.@Builder`) | Lombok Builder |
| **单例(枚举)** | 注册中心类型 (`AirServiceDiscoveryTypeEnum`) | 枚举常量绑定反射类 |

---

## 6. 线程安全设计

| 组件 | 线程安全机制 |
|------|-------------|
| 过滤器链映射 | `ReadWriteLock`：`buildAllFilterChainByStrategy` 写锁替换；`executeFilterChain` 读锁获取 |
| 策略内过滤器顺序 | `CopyOnWriteArrayList` |
| 过滤器链执行 | `CustomWebFilterChain` 每次请求新建，`AtomicInteger` 索引实例隔离 |
| 负载均衡轮询 | `AtomicLong` 计数器 (`AirRoundRobinStrategy`) |
| 令牌桶限流 | `AtomicReference<Double>` + `AtomicLong` + CAS |
| 服务发现管理器状态 | `volatile boolean initialized` |
| 注册中心客户端映射 | `ConcurrentHashMap<String, List<AirReactiveServiceDiscovery>>` |
| 日志异步发布 | `Schedulers.newBoundedElastic` 独立线程池 ("gateway-log") |
| 服务实例健康状态 | `volatile boolean registryHealthy / clientHealthy` |
| 健康检查结果缓存 | `ConcurrentHashMap` |

---

## 7. 异常体系

```
RuntimeException
  ├── AirCircuitBreakerOpenException      — 熔断器打开
  ├── AirRateLimitExceededException       — 限流触发
  └── AirServiceUnavailableException      — 后端服务不可用
```

---

## 8. 配置属性总览

| 前缀 | 绑定点类 | 来源 | 说明 |
|------|---------|------|------|
| *(server)* | application.yml | 仓库 | port: 10101 |
| `air-gateway.log` | `AirGatewayLogProperties` | 仓库 | 日志系统参数 |
| `discovery.registries` | `MultiAirRegistryCenterConfig` | Nacos | 多注册中心 |
| `flow-control` | `AirFlowControlConfigProperties` | Nacos | 流量控制管道 |
| `redisson` | `AirRedissonConfigProperties` | Nacos | Redis/Redisson |
| `mongodb` | `AirMongoDbConfigProperties` | Nacos | MongoDB 连接 |
| `kafka` | `AirKafkaConfigProperties` | Nacos | Kafka producer/consumer |
| `rocketmq` | `AirRocketMQConfigProperties` | Nacos | RocketMQ nameServer/producer/consumer |
| *(seata)* | bootstrap.yml | 仓库 | 分布式事务配置 |

---

## 9. 部署架构

```
┌──────────────────────────────────────────────────────────┐
│                    Nacos 集群                              │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐     │
│  │ 注册中心 │  │ 配置中心 │  │ 配置变更 │  │ 动态刷  │     │
│  │ (Nacos) │  │ (6个    │  │ 监听    │  │ 新      │     │
│  │         │  │  dataId)│  │         │  │         │     │
│  └─────────┘  └─────────┘  └─────────┘  └─────────┘     │
└──────────────────────────────────────────────────────────┘
       │                │               │
       ▼                ▼               ▼
┌──────────────────────────────────────────────────────────┐
│                 AirGateway 实例 (10101端口)                │
│                                                          │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐   │
│  │ 服务发现管理  │  │ 流量控制管道  │  │ RPC 客户端池  │   │
│  │ (兼容多注册中心)│ │ (限流/熔断)  │  │ (HTTP/gRPC/  │   │
│  │              │  │              │  │   RSocket)    │   │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘   │
│         │                 │                 │            │
│  ┌──────▼─────────────────▼─────────────────▼───────┐   │
│  │              Actuator / Prometheus                │   │
│  │   /actuator/health  /actuator/metrics            │   │
│  │   /actuator/prometheus  /actuator/custom         │   │
│  └──────────────────────────────────────────────────┘   │
└──────────────────────────────────────────────────────────┘
       │                │               │
       ▼                ▼               ▼
┌──────────┐    ┌──────────┐    ┌──────────┐
│ 后端服务  │    │  消息队列  │    │  缓存集群  │
│(多协议)   │    │Kafka/RocketMQ│ │Redis/Mongo│
└──────────┘    └──────────┘    └──────────┘
```

**外部依赖**：
- Nacos Server（注册中心 + 配置中心，默认 `127.0.0.1:8848`）
- Redis (Redisson) / MongoDB
- Kafka / RocketMQ
- Seata Server（分布式事务，配置已就位）

---

## 10. 待完善事项 (Roadmap)

| 模块 | 待完善内容 | 状态 |
|------|-----------|------|
| 网关引擎 | `AirGatewayEngine` 各路由方法接入真实后端转发 | mock 阶段 |
| RPC 客户端 | `HttpServiceClient` / `GRPCServiceClient` / `RSocketServiceClient` 的 execute 方法体 | 返回 null |
| RPC 请求构建器 | `HttpRequestBuilder` / `RSocketRequestBuilder` 方法体 | 全部返回 null |
| 流量控制 | `Resilience4jRateLimitOperator.apply()` 返回 null | 待接通 |
| 流量控制 | `AirGeneralDegradeOnConditionOperator` 仅类壳 | 待实现 |
| 流量控制 | `AirSlidingWindowRateLimitOperator` 需确认完整性 | 待验证 |
| 负载均衡 | `AirGatewayLoadBalancer` / `AirGatewayRouteLocator` 桩接口 | 待实现 |
| 过滤器策略 | `ApiFilterStrategy.flushStrategyOrder()` 待实现热刷新逻辑 | TODO |
| 注册中心 | Consul / Eureka 发现客户端方法体待实现 | 桩实现 |
| 降级 | `AirServiceInstanceHealthCheckService.getFallbackInstances()` 返回空 | 待实现 |
| 日志自动配置 | `AirGatewayLogAutoConfiguration` Bean 定义全部注释 | 待启用 |
| 日志存储 | `DefaultAirGatewayLogStorage` 具体实现待验证 | 待验证 |
| 配置变更监听 | 仅 RocketMQ 有监听器，Redis/MongoDB/流量控制配置变更监听缺失 | 待补充 |
| 消息队列 | `AirReactiveKafkaLogListener` 等待确认 | 待验证 |
| 测试 | 无 JUnit 测试框架接入 | 待补充 |

---

## 附录 A：包结构总览

```
com.airfree
├── AirGatewayApplication.java          — 启动类
├── access/                            — 接入层 (AirGatewayEndpointConfig)
├── annotation/                        — 注解定义 (log/monitor/mq)
│   ├── log/AirGatewayLogAnnotation.java
│   ├── monitor/
│   └── mq/
├── aspect/                            — 切面 (AirGatewayLogAspect)
├── cache/                             — 缓存子系统
│   ├── core/                          — 缓存核心
│   ├── client/                        — 缓存客户端 (Caffeine/Redis/MongoDB)
│   ├── config/                        — 缓存配置 (redis/mongo)
│   ├── cacheEnums/                    — 缓存事件操作枚举
│   ├── caheEventPublisner/            — 缓存事件发布器
│   ├── entity/                        — 缓存实体
│   └── event/                         — 缓存事件
├── core/                              — 网关引擎层
│   └── engine/AirGatewayEngine.java
├── discovery/                         — 服务发现子系统
│   ├── cache/                         — 服务发现缓存
│   ├── config/                        — 注册中心配置
│   ├── core/                          — 发现管理器 + 工厂
│   ├── discoveryService/              — 各注册中心实现
│   ├── enums/                         — 注册中心类型枚举
│   ├── event/                         — 发现事件
│   ├── instance/                      — 服务实例领域对象
│   └── utils/                         — 环境工具
├── entity/                            — 公共实体 (log/request/response)
├── filter/                            — 过滤器引擎
│   ├── customizeFilter/               — 自定义过滤器
│   ├── filterChain/                   — 自定义链
│   ├── filterEnums/                   — 协议枚举
│   ├── filterManager/                 — 过滤器管理器
│   ├── filterStrategy/                — 过滤器策略
│   └── globalFilter/                  — 全局过滤器
├── flow/                              — 流量控制子系统
│   ├── config/                        — 流量控制配置
│   ├── core/                          — 管道构建器 + 工厂
│   ├── enums/                         — 操作符类型枚举
│   ├── exception/                     — 流量控制异常
│   └── operator/                      — 操作符实现
├── health/                            — 健康检查子系统
│   ├── config/                        — 健康检查配置
│   ├── core/                          — 健康检查服务
│   ├── entity/                        — 健康实体
│   ├── enums/                         — 策略枚举
│   └── event/                         — 健康事件
├── listener/                          — 事件分发 + 配置监听
│   ├── configChangeListener/          — Nacos 配置变更监听
│   └── AirGatewayEventDispatcher.java
├── log/                               — 日志子系统
│   ├── config/                        — 日志配置 + 自动配置
│   ├── core/                          — 日志发布器 + 上下文 + 存储
│   ├── event/                         — 日志事件
│   └── logEnums/                      — 日志枚举
├── monitor/                           — 监控子系统
│   ├── actuator/                      — 自定义时钟
│   ├── collector/                     — 指标采集器
│   ├── config/                        — 监控配置
│   └── endPoint/                      — 自定义 Actuator 端点
├── mq/                                — 消息队列子系统
│   ├── cofig/                         — MQ 配置 (kafka/rocketMq)
│   ├── core/                          — 生产者/消费者工厂 + 监听器接口
│   └── index/                         — 消费者/生产者索引
├── router/                            — 路由子系统
│   ├── core/                          — 多注册中心路由定位器
│   └── tools/                         — 路由匹配器
├── rpc/                               — RPC 客户端层
│   ├── client/                        — 多协议客户端 + 建造器
│   ├── core/
│   ├── entity/                        — 请求上下文 + 端点 + 响应
│   └── enums/                         — RPC 协议枚举
├── security/                          — 安全（预留空包）
├── transaction/                       — 事务（预留空包）
└── utils/                             — 工具类
```

---

## 附录 B：关键配置示例

### B.1 Nacos 流量控制管道配置 (`airFlowControl.yaml`)

```yaml
flow-control:
  pipelines:
    order-process:
      - operator: rateLimit
        algorithm: Resilience4j
        resource: order.create
        config:
          limitForPeriod: 50
          limitRefreshPeriod: 50000
          timeoutDuration: 5
          writableStackTraceEnabled: true
        orderId: 0
      - operator: circuitBreaker
        algorithm: Resilience4j
        resource: order.service
        config:
          failureRateThreshold: 10
          waitDurationInOpenState: 30000
          permittedNumberOfCallsInHalfOpenState: 50
          slidingWindowSize: 60
          minimumNumberOfCalls: 5
        orderId: 3
```

### B.2 多注册中心配置 (`airDiscoveryConfig.yaml`)

```yaml
discovery:
  registries:
    enabled-registries:
      - nacos-primary
      - zookeeper-backup
    configs:
      nacos-primary:
        type: nacos
        server-addresses:
          - 127.0.0.1:8848
        namespace: 72461fe0-689e-45fe-bacd-aed8948f99da
        group: AIR_GROUP
        enabled: true
      zookeeper-backup:
        type: zookeeper
        server-addresses:
          - "127.0.0.1:2182"
        enabled: true
```

---

*文档生成日期：2026-07-02*
*基于源码静态分析自动生成，覆盖 134 个 Java 源文件。*